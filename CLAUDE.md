# Body Atlas — AIエージェント向けの作業メモ

「身体の哲学と科学」の通史サイト。Astro + Markdown + Cloudflare Workers。

## 作業前に読むもの

- `CONTRIBUTING.md` — コンテンツ追加の手順とルール（**必読**）
- `src/content.config.ts` — スキーマの唯一の仕様
- `templates/` — 新規ページのひな形

## コンテンツを追加・編集したら必ず

```bash
npm run validate   # 参照整合性の検査
npm run build      # validate + ビルド
```

両方通るまで完了としない。

## 守るべき編集方針

1. **関係の種類と確度を落とさない。** `relatedPeople` には `relation` と `confidence` を必ず付ける。
   直接の影響関係が史料的に確認できない場合は `conceptual_parallel` + `confidence: medium` にし、
   `note` に理由を書く。思想史を一直線に単純化しない。

2. **`reviewStatus: reviewed` を勝手に付けない。** 人手で原典に当たった確認を経ていないページは
   `researched`（資料に基づくが未検証）または `draft` にする。`reviewed` にするのは人間の判断。

3. **確立した知見と論争中の主張を混ぜない。** 概念には `evidenceStatus` を必ず付ける。
   以下は「確立した知見」として書かないこと：
   - 社会的プライミング（パワーポーズ、顔面フィードバック、マクベス効果）— 大規模追試で再現失敗
   - ミラーニューロンの機能的意義（存在は事実、機能は論争中）
   - ソマティック・マーカー仮説
   - 自由エネルギー原理（反証可能性をめぐり論争中）
   - 腸脳相関のヒトへの外挿（動物での因果とヒトでの相関を混同しない）

   比較的堅実なのは、運動制御論（ベルンシュタイン以降）、固有受容感覚、内受容感覚の神経基盤。

4. **出典を必ず書く。** `sources` が空だと validate が警告する。
   一次資料・邦訳・論文を区別して記録する。

5. **東洋・日本の身体論を西洋の付録として扱わない。** 対等な系譜として記述する。

## 事実関係で迷ったら

推測で断定しない。年代・帰属・訳書情報に不確かさがあるときは、本文にその旨を書き、
`reviewStatus` を下げる。「諸説あり」と書くほうが、間違った確定情報を書くよりよい。

## 触ってよい／慎重に扱う箇所

- 自由に追加してよい：`src/content/**`（スキーマに従う限り）
- 変更時は影響範囲を確認：`src/content.config.ts`、`src/lib/vocab.ts`（語彙を消すと既存ページが壊れる）
- デザイントークンは `src/styles/global.css` の `:root` に集約されている
